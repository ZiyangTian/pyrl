

from gomoku import game
from gomoku import model
from gomoku import player
from gomoku import policy


def generate_equivalent_data(features, transforming_features):
    """

    :param features: A `dict` from feature name to feature tensor.
    :param transforming_features:
    :return:
    """
    pass







def run():
    config = Config
    game_setting = game.GameSetting()